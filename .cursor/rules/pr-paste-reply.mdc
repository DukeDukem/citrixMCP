---
description: When the user types PR, run the paste-reply flow (write reply into Sprinklr box). CRITICAL - ONE REPLY ONLY.
alwaysApply: true
---

# PR = Paste Reply (ONE CUSTOMER ONLY)

**Chat scope:** **Email processing agent chat only** (`EMAIL-PROCESSING-AGENT.md`). Instructions dashboard must not run PR — see `.cursor/rules/instructions-only-no-re-pr-lf.mdc`.

CRITICAL: This command MUST output ONE customer reply ONLY. Never merge or combine responses.
CRITICAL: Paste content must belong ONLY to the currently visible case and the single customer in that case.

**AI model gate:** If this session is **Grok** → STOP. Do not paste. Output: `ERROR: GROK FORBIDDEN FOR CASE WORK. Switch to Claude 4.6 Sonnet (or Composer 2.5), then resend PR.` Prefer **Claude 4.6 Sonnet**. See `.cursor/rules/ai-model-stay-auto.mdc`.

When the user prompts with PR/pr:

## BEFORE PROCEEDING:

1. Identify the **currently visible case in Sprinklr**.
   - By default, PR always refers to the email/case that is currently open and visible in the Sprinklr console (the active tab/thread).
   - Do **not** require the user to restate the case ID or customer name if exactly one case is open and active.

2. Verify ONE customer in context
   - Look at the active Sprinklr case (and, if needed, the previous RE output for that same case).
   - Confirm you see only ONE customer name for the currently visible case.
   - If TWO or more different customer names appear **for the visible case** → STOP and ask: Which customer response should I paste?
   - If any customer detail in draft text does not belong to the visible-case customer → STOP. Output: ERROR: CUSTOMER DATA MIX DETECTED. DO NOT PASTE.
   - If any customer-number or phone-number suffix in draft text does not match the visible-case customer identifiers → STOP. Output: ERROR: IDENTIFIER MISMATCH DETECTED. DO NOT PASTE.
   - If salutation name in draft does not match visible-case customer signature/identity → STOP. Output: ERROR: SALUTATION NAME MISMATCH DETECTED. DO NOT PASTE.
   - If no customer name is visibly verifiable in the active case, any named salutation is forbidden; use only `Guten Tag,` or STOP.

3. Verify ONE case ID
   - Confirm the active Sprinklr thread has only one Fall # / case ID.
   - If multiple case IDs are simultaneously in context (e.g. user has switched cases mid-flow or multiple IDs are shown) → STOP and ask which one should be used.
   - Confirm the reply source case ID (from latest RE output used for this PR) matches the currently visible Fall #.
   - If IDs differ → STOP and output: ERROR: CASE ID MISMATCH. Do not paste previous-case reply into current case.
   - If any second case reference is found in draft text/context -> STOP. Output: ERROR: MULTI-CASE CONTAMINATION DETECTED. DO NOT PASTE.

## THEN:

1. Use the suggested reply from the previous read-email (RE) output for ONE customer only
   - Source must be the latest RE for the currently visible case only.
   - Default binding rule: if PR is issued immediately after RE and no explicit rewrite instruction was provided by the user, paste the **same wording** from that RE section 6 (no paraphrase, no alternative draft). **Formatting for paste:** use **mail format** (previous length / paragraph layout / signature line breaks) — **not** the chat ~72-column soft-wrap used only for reading in Cursor. Rejoin chat soft-wraps within paragraphs before writing the reply file. Encoding and template structure stay the same. See `.cursor/rules/ENCODING-SAFE-OUTPUT.mdc`.
   - Hard gate: if latest RE case ID does not match the currently visible case ID, PR must STOP and refuse paste (never fallback to any other draft file).
   - Only two source types are allowed for PR (same visible case only):
     1) exact section-5 reply from latest RE (7-step output), or
     2) explicit user-directed replacement text provided after RE for that same case (**case exception** — does **not** rewrite TransferMatrix, KB, or standing rules; see `.cursor/rules/operator-case-exceptions.mdc`).
   - Any other payload source is forbidden and must be blocked.
   - Explicitly forbidden: inserting unrelated canned replies (e.g., generic cancellation/deactivation confirmations) that are not the case-locked RE reply or a case-locked user-directed override.
   - When the operator’s rewrite **differs from** matrix/KB/agent draft: apply it for **this Fall # only**; note `Case exception for #FALL_ID — operator PR override (does not change TM/KB/rules).` Do **not** carry that wording to the next case.
   - Do **not** reuse stale cached text, old temp files, clipboard remnants, or prior-case drafts.
   - Do **not** fuse/append text fragments from any other case under any circumstances.
2. BEFORE writing to file: do a final visual check
   - Count "Guten Tag" salutations — must be exactly 1
   - Verify salutation identity: named salutation must belong to visible-case customer; if uncertain, rewrite to `Guten Tag,`
   - Count signature blocks — must be exactly 1
   - Count survey lines ("Zur Verbesserung unseres Kundenservices ...") — must be exactly 1
   - Reject repeated paragraphs: if any paragraph block is duplicated or near-duplicated, STOP.
   - Verify identifier safety: no full customer number/phone number, and any suffix reference must belong to the visible case only.
   - Enforce no-uninvited-promises rule before paste: reject wording that promises internal checks, processing updates, or follow-up messages unless the user explicitly instructed those exact promises.
   - Enforce no-internal-systems rule: reject any mention of Authentifizierungsmatrix, Sabio, TIM, Wissensbasis, TransferMatrix, Sprinklr, Marquez, Themen-ID, or internal queue names (see `.cursor/rules/reply-no-internal-systems.mdc`). If found → ERROR: INTERNAL SYSTEM NAME IN CUSTOMER REPLY. DO NOT PASTE.
   - Enforce no-case-parroting rule: reject *„Sie haben mitgeteilt“* / *„Sie schreiben“* / long narrative recap before actionable content (see `.cursor/rules/reply-no-case-parroting.mdc`). If found → ERROR: CASE PARROTING IN CUSTOMER REPLY. DO NOT PASTE. Rewrite to one thank-you sentence + solutions.
   - If any check fails → REJECT and output: ERROR: DUPLICATE OR PROMISE-BLOCKED RESPONSE DETECTED. Rewrite to a single clean reply before pasting.
3. Write that ONE reply to a file (freshly generated for this case; never append to existing reply file)
4. Run the write-reply script with that file path and the --no-fill-case-tracker flag
   - If page-state detection is uncertain but the reply editor (`Nachricht verfassen`) is visible, proceed with single-shot paste (do not block on strict state label alone).
   - Mandatory write gate: the script must hard-clear editor first and verify the editor is truly empty before write. If not empty after retry, STOP with error and do not paste.
   - Mandatory duplicate-invocation gate: block near-immediate duplicate PR invocation for same case/reply (single-shot across process runs).
5. Immediately run post-paste verification (`verify_no_data_leak.py`).
6. If verification is not CLEAN:
   - Clear the editor completely.
   - Lock context to currently visible case only (discard any previous case context).
   - STOP immediately (no automatic second paste attempt).
   - Do not allow send.
   - Output: ERROR: POST-PASTE VERIFICATION FAILED. MANUAL CLEANUP REQUIRED.

## AFTER EXECUTION:

- ONE and only ONE customer reply has been pasted into Sprinklr
- No data leaks
- **User sends manually** via Sprinklr **Senden** (see below) — agent does **not** click Senden and does **not** treat post-send empty editor as a paste failure
- Write-reply flow terminates immediately after one paste + verification step (no repaste loop)
- **After PR succeeds (EMAIL non-transfer):** Paste + verify is the entire PR action. **No arm command here.** `--closeout-anwenden` was already started at Auto-LF time (right after the 7-step). Finishing quote: `PR done for #FALL_ID`.
- Agent does **not** run `--closeout-anwenden` at PR time. Arm is already live in the background.
- **PR LF** remains valid **recovery** if Auto-LF was skipped (PR then LF then arm).
- Transfer cases: no PR; arm already started at Auto-LF time.

**Hard rule — Senden is NOT part of arming and has NO ordering requirement with Anwenden:**
- Senden = sends the reply email to the customer. Completely independent.
- Anwenden = closes the Sprinklr case. Closeout arm already watches for this from Auto-LF time.
- Operator may click Senden before Anwenden, after, or not at all — arm does not care.
- Agent must **never** say "Senden, then Anwenden" or imply any ordering.

### User clicks **Senden** (not an arm; not a paste error)

After a **CLEAN** post-paste verification, the user sends manually. Sprinklr often needs **two clicks** on the same primary reply button:

| Step | Button label (typical) | Selector (stable) |
|------|------------------------|-------------------|
| 1 | **Senden** | `button[data-testid="replyBox-sendBtn"]` + `data-tracker-event-id="@replyBox/primaryAction/replyButton"` |
| 2 (if grammar gate appears) | **Ignorieren und senden** (same testid; label/position may change) | Same `replyBox-sendBtn` / `@replyBox/primaryAction/replyButton` |

**Grammar warning (normal, not a real error):** After the first **Senden**, Sprinklr may flag “potential grammatical errors.” That is **Sprinklr’s internal grammar criteria**, not proof the agent pasted bad German and not a failed PR. The user dismisses by clicking the button again when it shows **Ignorieren und senden**.

### Additional send confirmation — outbound bubble in conversation tray

After send succeeds, Sprinklr shows the agent’s reply in the **left conversation / chat tray** as a brand outbound item. Treat this as positive proof the PR was sent:

| Signal | Selector / where |
|--------|------------------|
| Brand outbound item | `div[data-testid="inboundChatConversationItemBrandMessage"]` |
| Email body container inside it | `div[data-element-type="email-message-container"]` with `html-message-content` |
| Message type | Often `data-testid="messageType"` → **Antwort** |
| Sent time (top-right of bubble) | e.g. `span` with text like `9 Sep. 26, 08:11` next to Betreff |

**Agent check (optional, when unsure):** If the reply box is empty after Senden and a new `inboundChatConversationItemBrandMessage` appears whose `html-message-content` matches the pasted reply (salutation / key lines) and whose timestamp is near the PR paste time → **email was sent**. Do **not** treat this as a new inbound customer mail or as a failed paste.

**Meaning for the agent:**

1. Until the second confirm (or a single Senden with no gate), the mail may still be unsent — editor can still show the reply. That is OK.
2. After the user finishes send (Senden, or Senden → Ignorieren und senden), the email **was sent** — PR paste **succeeded**.
3. The reply editor becoming **empty** (or compose UI resetting) after that is **expected** — **not** `ERROR: POST-PASTE VERIFICATION FAILED`, not “paste didn’t stick”, not a reason to re-PR.
4. A new **brand** tray bubble (`inboundChatConversationItemBrandMessage`) with matching content/timestamp is **extra confirmation of send success**.
5. **Neither Senden nor Ignorieren und senden is part of arming** (not Anwenden / Next / Weiter / Weiterleiten). Do **not** watch these buttons to trigger `--arm*`. Arm (`--closeout-anwenden`) was already started at Auto-LF time — independent of PR and Senden.
6. Do **not** re-run leak verification against an empty box after send and treat emptiness as failure.
7. Do **not** “fix” the reply just because Sprinklr showed the grammar gate.
8. If CLEAN was already returned for this PR, the paste is done. Arm (`--closeout-anwenden`) was already running from Auto-LF — no further action needed even if editor is empty.

VIOLATION OF THIS RULE = DATA BREACH. DO NOT PROCEED IF UNCERTAIN.


