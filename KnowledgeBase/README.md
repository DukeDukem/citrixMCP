# Knowledge Base (Markdown)

Converted from Word (`.docx`) files in:
`C:\Users\PC ENTER\Desktop\YOUMMDAY IMPORTANT SHIT\KNOWLEDGE BASE`

**To re-run conversion** (from project root):
```bash
uv run python scratchspace/convert_docx_to_markdown.py
```
Requires: `mammoth`, `html2text` (`uv pip install mammoth html2text`).

These Markdown files can be referenced and searched when prompted.
